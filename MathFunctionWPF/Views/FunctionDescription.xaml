<UserControl x:Class="MathFunctionWPF.Views.FunctionDescription"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MathFunctionWPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="176" d:DesignWidth="1245">
    <Grid>
        <TextBlock HorizontalAlignment="Left" Margin="188,0,0,0" TextWrapping="Wrap"  VerticalAlignment="Center" Height="96" Width="783"
                   
                   Text="Метод дихотомии (бисекции) — это численный метод для нахождения корня функции, основанный на теореме о промежуточном значении, которая утверждает, что если непрерывная функция на отрезке 
[
𝑎
,
𝑏
]
[a,b] принимает значения разных знаков на концах этого отрезка (т.е. 
𝑓
(
𝑎
)
⋅
𝑓
(
𝑏
)0f(a) f(b) то внутри отрезка существует как минимум один корень." />

    </Grid>
</UserControl>
